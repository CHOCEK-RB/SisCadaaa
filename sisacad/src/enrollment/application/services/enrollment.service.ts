import { Injectable } from '@nestjs/common';

/**
 * @class EnrollmentService
 * @description
 * This service currently acts as a placeholder for core enrollment business logic.
 * Future implementations might include methods for creating, updating, or managing
 * enrollment statuses beyond what is covered by other specialized enrollment services.
 */
@Injectable()
export class EnrollmentService {
  constructor() {}
}
